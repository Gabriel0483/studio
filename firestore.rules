
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // ----------------------------------------------------------------------
    // Helper Functions
    // ----------------------------------------------------------------------

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function getStaffData() {
      return get(/databases/$(database)/documents/staff/$(request.auth.uid)).data;
    }

    function isPlatformAdmin() {
      // Emergency escape hatch or platform-wide admin (optional)
      return isSignedIn() && request.auth.token.email == 'rielmagpantay@gmail.com';
    }

    function isAnyAdmin() {
      return isSignedIn() && (isPlatformAdmin() || exists(/databases/$(database)/documents/staff/$(request.auth.uid)));
    }
    
    function isSuperAdmin() {
      return isPlatformAdmin() || (isAnyAdmin() && exists(/databases/$(database)/documents/staff/$(request.auth.uid)) && getStaffData().roles.hasAny(['Super Admin']));
    }

    // Helper to verify data belongs to the staff's tenant
    function isTenantData(resourceData) {
      return isPlatformAdmin() || (isAnyAdmin() && exists(/databases/$(database)/documents/staff/$(request.auth.uid)) && (getStaffData().tenantId == resourceData.tenantId));
    }

    // ----------------------------------------------------------------------
    // Collection Rules
    // ----------------------------------------------------------------------
    
    match /tenants/{tenantId} {
      allow read: if true;
      allow write: if isPlatformAdmin();
    }

    match /staff/{staffId} {
      allow get: if isOwner(staffId) || isAnyAdmin();
      allow list: if isSuperAdmin();
      // Only same-tenant staff can be seen by a tenant super admin
      allow write: if isPlatformAdmin() || (isSuperAdmin() && exists(/databases/$(database)/documents/staff/$(request.auth.uid)) && getStaffData().tenantId == request.resource.data.tenantId);
    }

    match /passengers/{passengerId} {
      allow get: if isOwner(passengerId) || isAnyAdmin();
      allow list: if isAnyAdmin();
      allow create: if isOwner(passengerId) || isAnyAdmin();
      allow update: if isOwner(passengerId) || isAnyAdmin();
    }
    
    match /announcements/{id} {
      allow read: if true;
      allow write: if isTenantData(request.resource.data) || (resource != null && isTenantData(resource.data));
    }
    
    match /ports/{id} {
      allow read: if true;
      allow write: if isTenantData(request.resource.data) || (resource != null && isTenantData(resource.data));
    }

    match /ships/{id} {
      allow read: if isTenantData(resource.data);
      allow write: if isTenantData(request.resource.data) || (resource != null && isTenantData(resource.data));
    }

    match /routes/{id} {
      allow read: if true;
      allow write: if isTenantData(request.resource.data) || (resource != null && isTenantData(resource.data));
    }

    match /fares/{id} {
      allow read: if true;
      allow write: if isTenantData(request.resource.data) || (resource != null && isTenantData(resource.data));
    }

    match /schedules/{id} {
      allow read: if true;
      allow write: if isTenantData(request.resource.data) || (resource != null && isTenantData(resource.data));
    }

    match /bookings/{id} {
      allow get: if isOwner(resource.data.passengerId) || isTenantData(resource.data);
      allow list: if isAnyAdmin(); // Filtered client-side
      allow create: if isSignedIn();
      allow update: if isTenantData(resource.data);
      allow delete: if isTenantData(resource.data);
    }

    match /maintenanceRecords/{id} {
      allow read, write: if isTenantData(resource.data) || isTenantData(request.resource.data);
    }
    
    match /boarding/{id} {
      allow read, write: if isAnyAdmin(); // Filtered client-side
    }
  }
}
